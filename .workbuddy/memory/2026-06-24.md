# 2026-06-24 工作日志

## GitHub 代码推送
- 全部代码已提交并推送至 GitHub：1634189812/zyuit-website
- 远程地址改为 HTTPS+token（SSH 被沙箱墙），后续 `git push` 可直接使用
- .gitignore 排除辅助脚本（create_favicon.py, draw_mflex_topo.py）和 .workbuddy 目录

## Git 风险教训
- 每次代码修改后必须 git add + commit + push，绝不能只留本地
- 回滚依赖 git 历史，本地无提交则无法恢复
- 创建仓库 1634189812/zyuit-website
- 因 HTTP 被墙，改用 SSH Key 方式推送成功
- 配置 core.sshCommand 指向专用密钥 `~/.ssh/id_ed25519_github`

## 腾讯云服务器选购指导
- 确认用户购买 Lighthouse 轻量服务器 2核2G 192元/年（新客）
- 建议地域选上海（匹配江苏备案）
- 镜像选 CentOS 7.9 或 Ubuntu 22.04（纯净系统）

## 拓扑图优化
- **KSRCB**: 完全重建拓扑图（PIL绘图），去掉所有POLYCOM/宝利通旧设备
  - 重新设计为YMS 3500 3+N集群架构，无第三方品牌残留
- **全部4个拓扑图**: 2x放大 + 锐化 + 添加"江苏云科网数"斜纹水印
- 输出分辨率: KSRCB 2400x1700 / Bosideng 3588x2066 / MFLEX 2868x1780 / JCET 3840x2604

## 隐私脱敏
- KSRCB: 花桥→总部数据中心, 青阳路→总部附属楼, 去掉POLYCOM/MAXHUB/Cisco品牌名
- MFLEX: 红庄→节点A, 郭巷→节点B
- JCET: 临港→上海新工厂/上海新建
- 已验证全部4个HTML，无敏感位置信息残留

## 第二轮拓扑优化 + 配色升级
- **KSRCB拓扑完全重绘**（PIL原生绘图 2800x1800）：简洁清晰，YMS3500×3集群 → IP网络 → 36支行(MeetingBoard) + 右侧总部会议室终端
- **全部4张拓扑图统一加水印**："江苏云科网数通信技术有限公司"全称斜纹水印
- **4个案例页面去黑增活力**：
  - Hero背景: 暗色(#0d1b2a/#1a1a2e等) → 亮蓝色(#1a73e8→#0d5fc5)
  - Footer: #111 → #0d3a6e
  - JCET sec-dark: #111 → #0d3a6e
  - jcet-topo从jpg升级为png
- GitHub推送 + CloudStudio沙箱部署成功

## 第三轮：密集斜纹水印全覆盖
- 用户要求水印铺满全图（类似参考图效果）
- 大幅提高水印密度：x_step = tw*0.55, y_step = th*0.75
- 全部4张拓扑图重新生成，GitHub推送+沙箱部署

## 第四轮：撤回水印 + 腾讯云服务器部署
- 用户不满意水印效果，全部撤回为高清无印原图（GitHub恢复+重新生成）
- 用户购买腾讯云 Lighthouse：南京2核2G 192元/年，Ubuntu 22.04，IP: 146.56.231.87
- SSH密钥绑定 ubuntu 用户，密码 Admin85@2007
- 部署完整流程：
  1. apt update + upgrade（内核升级到6.8.0-124）
  2. 安装 Node.js 22.23.0（NodeSource源）
  3. 安装 Nginx 1.24.0
  4. 安装 PM2（全局）
  5. SFTP上传全部网站文件到 /home/ubuntu/zyuit-website/
  6. npm install express（修复缺少node_modules导致PM2重启15次的问题）
  7. Nginx反向代理配置：静态文件直出 + /api/ 代理到 Node:3000
  8. 修复 /home/ubuntu 目录权限（需 o+x 让 www-data 遍历）
  9. PM2守护 + systemd开机自启
- 表单提交改为同域 /api/contact 代理（不再依赖 Cloudflare Workers，彻底解决CORS）
- 验证通过：首页200、案例页200、API 200

## 第五轮：KSRCB拓扑图v2参照公安系统风格重绘
- 用户参考公安系统拓扑图风格，要求白底浅蓝专业风格
- 重新设计2400x1600拓扑图：
  - 顶部：数据中心（YMS 3500 ×3 节点A/B/C 3+N高可用集群）
  - 中间：云状IP网络
  - 底部左：36家支行终端部署（MeetingBoard 65/75/86 Pro）
  - 底部右：总部会议室终端（MeetingEye 500 Pro ×5 + MeetingBar A40 + UME会管系统）
- 修复字体问题：改用Windows系统微软雅黑（msyhbd.ttc/msyh.ttc）
- 上传至腾讯云服务器 + GitHub推送 + CloudStudio沙箱部署
- 用户域名确认：yunkct.com（非zyuit.com），正在走ICP备案流程

## 第六轮：企业微信推送格式优化
- 用户反馈markdown推送太简陋
- 改为 textcard 卡片格式：标题 `官网咨询 · 姓名 · 公司` + HTML高亮字段 + 「查看官网」可点击按钮
- server.js 更新后上传至腾讯云 + PM2 restart (进程名zyuit)
- GitHub已提交

## 第七轮：记忆更新
- 移除回复末尾自动附带沙箱地址的规则

## 第八轮：企业微信推送卡片优化
- 用户反馈需求摘要字体太大（emphasis_content导致），点击跳转yunkct.com域名未备案无法访问
- 去掉 emphasis_content，所有字段统一字号（horizontal_content_list）
- card_action.url 改为 http://146.56.231.87（IP地址，域名备案完成后改回）
- 删除未使用的 msgShort 变量
- 上传 server.js 至 /home/ubuntu/zyuit-website/，pm2 restart zyuit
- 接口验证通过

## 第九轮：去掉跳转 + 线索管理后台
- 用户反馈 card 点击跳转网站无意义，去掉 card_action
- 发现企业微信 template_card 强制要求 card_action 字段（42045错误），改为跳转管理后台
- 新建线索存储系统：leads.json 从 object 格式迁移为 array，每条款带 id/name/company/phone/position/email/interest/message/status/progress/notes
- 新建线索管理后台页面 /admin.html：
  - 令牌鉴权（token=yunkct2024）
  - 表格展示全部线索：编号/时间/姓名/公司/电话/关注方向/需求/状态/进度/备注
  - 状态下拉：待联系/已联系/已报价/已成交/已放弃
  - 进度、备注可内联编辑，保存按钮
  - 顶部统计栏：全部/待联系/已联系/已成交
  - 搜索过滤 + 状态筛选
  - 企业微信卡片增加 jump_list 入口：「查看线索管理后台」
- 新增 API：GET /api/leads（列表）、PUT /api/lead/:id（更新状态/进度/备注）
- 部署验证通过，数据正常写入 leads.json

## 第十轮：多用户登录 + 权限分级
- 管理员后台改为用户名密码登录（不再用 URL token）
- 创建三个账号：
  - admin / admin@123 — 管理员，拥有删除权限
  - 01 / admin@123 — 普通成员
  - 02 / admin@123 — 普通成员
- 普通成员可查看/编辑线索（状态/进度/备注），不可删除
- 管理员额外显示「删除」按钮，点击后弹出确认框
- 新增 API：POST /api/login, GET /api/session, POST /api/logout, DELETE /api/lead/:id
- 登录态 8 小时过期，支持 sessionStorage 自动恢复
- 企业微信卡片跳转链接改为 admin.html（需手动登录）

## 第十一轮：前端高级动画效果
- 引入三个顶级动画库（CDN加载）：
  - GSAP 3.12 + ScrollTrigger — 滚动触发动画引擎
  - tsParticles 2 — 粒子效果引擎
- Hero 区域：tsParticles 科技感网络节点粒子背景 + GSAP 交错文字入场（blur + scale）
- 滚动入场：fade-up/slide-left/slide-right 元素通过 ScrollTrigger 自动触发
- 产品卡片：每个品牌区域卡片错落入场 + 3D 透视 hover 倾斜 + 图片微放大
- 案例 logo：弹性回弹入场动画（back.out）
- 服务优势：数字缩放弹性动画 + 逐项 cascade
- 导航栏：滚动超过60px后毛玻璃半透明背景 + 阴影
- 合作客户 logo：淡入效果
- Footer：滚动淡入

## 第十二轮：翻页渐变滚动特效
- 用户建议："往下滑动的时候，以翻页渐变的效果会不会更好"
- 将所有板块（about/cases/products/clients/advantages/contact/footer）改为 CSS clip-path 翻页式揭开
- 原理：`clip-path: inset(0 0 100% 0)` → `inset(0 0 0% 0)`，内容从下往上像翻书一样揭开
- 板块标题（kicker/h2/p）加入 fade-up 类，在翻页揭开后逐层浮现
- 产品卡保持 3D 倾斜 + 品牌级交错入场
- 案例卡保持弹性弹入
- 优势卡修复了 fromTo + onEnter 双重触发冲突，改用 gsap.set 初始态 + onEnter 播放

## 第十三轮：MFLEX拓扑图高清重绘 + 脱敏
- 用PIL重新绘制MFLEX/维信电子UC双中心语音通信拓扑图，分辨率2800x1800
- 脱敏处理：郭巷3+0中心→A 3+0中心，红庄3+0中心→B 3+0中心；郭巷工厂→A工厂，红庄工厂→B工厂
- 拓扑结构：运营商E1×2 → YMG200网关×2 → 内网 → 语音网关+UC音视频服务器 → A/B双中心 → 公司机房(本地工厂) + A/B远程工厂(SIP Trunk接入)
- 配色统一蓝/绿/紫/橙专业色系，带图例
- 上传至服务器替换旧图，140KB PNG

## 第十四轮：标签页favicon图标
- 所有HTML页面（index.html + 4个案例页 + admin.html）添加 favicon 链接
- 使用 logo-new.png 作为图标，替换浏览器默认的地球图标
- 部署至服务器，logo-new.png 同步上传
- Apple极简全屏滚页版被用户否决（"好丑"），git restore 回老版本后完整重建翻页渐变版
- 手动重建: GSAP clip-path 翻页 + tsParticles 粒子 + 3D 卡片 tilt + elastic bounce + favicon

## 第十五轮：线索管理后台优化
- 添加"职位"列（公司后）
- 进度、备注改为点击弹出大窗编辑（600px宽弹窗，200px+ textarea，Ctrl+Enter保存）
- 表格中进度/备注显示为短文本摘要，点击即弹窗

## 第十六轮：MFLEX拓扑图替换 + 首页Apple极简改版
- 用户直接提供新拓扑图截图，替换 case-mflex-topo.png
- 首页完全重写为 Apple 极简全屏滚页风格：
  - scroll-snap 全屏吸附滚动
  - Hero：深蓝渐变 + 大标题 + gradient文字 + 简洁CTA
  - 毛玻璃导航（滚动后 backdrop-filter blur）
  - 产品卡片：按品牌分组白底圆角卡片阵
  - 案例卡片：logo + 标签 + hover浮起 + 箭头
  - 服务优势：3×2网格数字卡片
  - 联系表单：左右分栏布局
  - 去掉GSAP/tsParticles CDN依赖，改用纯CSS scroll-snap + IntersectionObserver动画
  - 加载过渡动画
