# 2026-06-30 工作日志

## 网站安全整改
- 根据安全自查报告，修复了可立即操作的安全隐患
- HTTPS 暂未部署（域名 ICP 备案未完成，无法申请证书）

### 1. Nginx 安全加固
- 添加 `server_tokens off;`（隐藏 Nginx 版本号，避免信息泄露）
- 添加安全响应头：
  - `X-Frame-Options: SAMEORIGIN`（防点击劫持）
  - `X-Content-Type-Options: nosniff`（防 MIME 嗅探）
  - `X-XSS-Protection: 1; mode=block`（防 XSS 反射攻击）
  - `Referrer-Policy: strict-origin-when-cross-origin`（控制 Referer 泄露）
- 安全头在 HTML location、location /、静态资源 location 中均已覆盖
- 验证：所有页面均正确返回安全响应头
- 配置文件已保存到仓库 nginx_zyuit.conf（参考用），部署后删除本地副本

### 2. 后台管理员密码更换
- 从弱密码 `admin@123` 更换为 `Admin85@2007`（用户指定）
- server.js 已更新并部署，PM2 重启成功

### 3. 系统安全补丁更新
- `sudo apt update && sudo apt upgrade -y` 完成
- 更新了 14 个包，包括 nodejs 22.23.0→22.23.1、perl、kernel microcode、sqlite3、nss、tar 等
- 需重启以加载新内核微码（已标记 REBOOT_NEEDED，暂未重启，择机执行）

### Git
- Commit 58a47e6 + push to GitHub main 分支
